# 배포 런타임(app/)에 실제로 필요한 의존성만 둔다 — deploy/backend.Dockerfile이 이 파일만
# 설치한다. 크롤링/색인 스크립트(scripts/)용 무거운 패키지는 requirements-tools.txt로 분리했다
# (docling이 torch를 끌고 와 그것만 1GB 가까이 되는데, EC2 런타임에선 쓰이지 않는다).

# Web framework
fastapi
uvicorn[standard]

# HTTP client (FSS Open API)
httpx

# Embeddings
openai
tiktoken

# 한국어 형태소 분석 (하이브리드 검색의 sparse/BM25 토크나이저)
kiwipiepy

# Vector DB
qdrant-client

# Object storage (크롤링한 PDF 원본). minio=로컬 docker-compose, boto3=배포(AWS S3)
minio
boto3

# Config
python-dotenv
