TODO

1. PDF 추출할 때, 테이블 잘 구조화해서 임베딩하기

2. 실제 데이터 수집하는 수집 파이프라인 구성하기 (스크립트)
   [1. Target URL 수집] ──► [2. PDF 다운로더] ──► [3. PDF Text Extractor] ──► [4. Cleaning & Chunking]
   (공시실 게시판 파싱) (httpx / Playwright) (PyMuPDF / LlamaParse) (Markdown 변환 / Vector DB)

3. 지금 RAG + AI 파이프라인은 진짜 scaffold니까 차후에 로직은 개선 필요

4. 마이데이터에서 실제 의미 있는 데이터 정제해서 가져오는 로직 추가 필요

- 현재는 유동성/자산분포 2개만 우선 구현 완료. 우대금리(카드 실적 집계)는 보류 — 실사용해보고 필요하면 추가.

5. PDF 약관 날짜별로 잘 받아서, 실제 적용되는 약관 / 적용 안되는 약관 구분하기

6. TODO: crawled_data PDF 원본을 S3(또는 다른 오브젝트 스토리지)로 옮기기
   - 지금은 로컬 파일로 충분 (런타임엔 Qdrant 임베딩만 있으면 되고, PDF는 임베딩 파이프라인용 빌드타임 재료라 서빙 안 함)
   - "원문 PDF 보기" 같은 기능이 실제로 필요해지면 그때 검토

---

DONE (7/24)

1. 우선 RAG + LLM 파이프라인 연결 자체는 완료
2. 금융상품 openAPI 연결 완료
3. 마이데이터 mock DATA는 현재 schema만 정의 완료. 추가적으로 실제 응답은 어떤 식으로 받아야할지 고민중.
   => 그럴듯 하게 보이면서, 너무 과하지 않게 하고 싶은데, client method를 어떻게 받아야 할지..
   => 우선 예/적금 상품 한정으로 scope 좁히고, 조회하는 거는 = 우대 금리 달성 여부 / 중도 해지 위험 (현금 흐름 및 유동성) / 자산 분포 데이터 (예금자 보호 초과)그런ㄱ

해야 할 거

3. PDF 추출할 때, 테이블 잘 구조화해서 임베딩하기 => RAG 저장 쪽 구현 (PDF 파싱 파이프라인 구현 (서로 다른 PDF에서 잘 파싱해서 텍스트화?) + 해당 PDF 내용 기반으로 metadata 붙이는 로직 구현)
4. 환각(Hallucination) 방지 관련 구현
5. 프론트엔드 수정 => 실제 상품 나오도록 UI 수정 & 결과 받아서 팝업 식으로 뜨도록 수정 + UX 고려해서 유저 시나리오처럼 설계

6. S3 도입해서 실제 PDF 저장 => 원문 약관/설명서 보면서 어디 출처로 했는지 확인 (UX 개선)
7. 웹 서치 fallback 도입
