TODO

1. PDF 추출할 때, 테이블 잘 구조화해서 임베딩하기

2. 실제 데이터 수집하는 수집 파이프라인 구성하기 (스크립트)
   [1. Target URL 수집] ──► [2. PDF 다운로더] ──► [3. PDF Text Extractor] ──► [4. Cleaning & Chunking]
   (공시실 게시판 파싱) (httpx / Playwright) (PyMuPDF / LlamaParse) (Markdown 변환 / Vector DB)

3. 지금 RAG + AI 파이프라인은 진짜 scaffold니까 차후에 로직은 개선 필요

---

DONE (7/24)

1. 우선 RAG + LLM 파이프라인 연결 자체는 완료
2. 금융상품 openAPI 연결 완료
3. 마이데이터 mock DATA는 현재 schema만 정의 완료. 추가적으로 실제 응답은 어떤 식으로 받아야할지 고민중.
   => 그럴듯 하게 보이면서, 너무 과하지 않게 하고 싶은데, client method를 어떻게 받아야 할지..
   => 우선 예/적금 상품 한정으로 scope 좁히고, 조회하는 거는 = 우대 금리 달성 여부 / 중도 해지 위험 (현금 흐름 및 유동성) / 자산 분포 데이터 (예금자 보호 초과)그런ㄱ

내일 해야 할 거

1. 마이데이터 기반으로 유저 정보 mock data 받아오는 거 진행
2. 실제 금융사 홈페이지 가서 상품설명서, 약관 등등 크롤링해오는 스크립치트 (크롤링 스크립트에서 pdf + metadata까지 만들어주면 편할 듯)
3. PDF 파싱 파이프라인 구현 (서로 다른 PDF에서 잘 파싱해서 텍스트화?) + 해당 PDF 내용 기반으로 metadata 붙이는 로직 구현
4. 프론트엔드 수정 => 실제 상품 나오도록 UI 수정 & 결과 받아서 팝업 식으로 뜨도록 수정 + UX 고려해서 유저 시나리오처럼 설계
