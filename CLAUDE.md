# FinMark (핀마크) — 프로젝트 기획서

> KB AI Challenge 해커톤 프로젝트. 이 문서는 기획/아키텍처/기술스택 정리본이며, 세션이 바뀌어도 컨텍스트를 빠르게 복원하기 위한 용도.

## 1. 개요 & 개발 배경 (Problem & Target)

**Target**: 약관을 꼼꼼히 읽을 시간이 없고 금융 지식이 부족한 사회 초년생

**Problem**

- '최고 연 6%' 같은 금리 숫자만 보고 가입하지만, 정작 우대 조건·중도해지 불이익·예금자보호 한도 같은 건 빽빽한 약관 뒤에 숨어 있어 확인하지 못함.
- 기존 AI/검색 서비스는 소비자가 **무엇을 물어봐야 할지 미리 알아야** 답해줌. 정작 모르는 사람은 질문 자체를 못 함.

**Solution**

- 상품을 고르면 AI가 그 상품의 **약관·상품설명서를 먼저 읽고**, ① 알아야 할 상품 정보와 ② 체크해야 할 위험 요소를 쉬운 말로 정리해 줌.
- 유저의 마이데이터(자산·소비)를 자동으로 결합해, **"이 사람에게 해당되는 것"**만 짚어줌.
- 모든 항목에 근거 조항을 달고, 클릭하면 **원문 PDF의 해당 문장에 밑줄(하이라이트)**을 쳐서 보여줌 — 서비스명이 여기서 나옴.

## 2. 핵심 UX & 시연 시나리오 (User Journey)

같은 상품을 골라도 유저의 마이데이터에 따라 리포트 내용이 달라짐.

```
[금융 포털에서 상품 선택 → "AI에게 물어보기"]
                            │
                            ▼
                 [FinMark AI 스캔]
                            │
       ┌────────────────────┴────────────────────┐
       ▼                                          ▼
[유저 A: 잔고 20만원, 순현금흐름 -50만]   [유저 B: 한 은행에 1.3억 집중]
       ▼                                          ▼
 "중도해지 시 이자 손실"                    "예금자보호 한도 초과"
 잔액이 200,000원이라 급전이                 KB국민은행에 130,000,000원이
 필요하면 중도해지이율이 적용됨               예치되어 1억 초과분은 미보호
       │                                          │
       └────────────────────┬─────────────────────┘
                            ▼
        [근거 조항 클릭 → 원문 PDF의 해당 문장 하이라이트]
```

데모 유저 4명 (`app/data/mydata_mock.json`) — 각각 유저특화 검색 축을 하나씩 트리거하도록 설계:

| 유저 | 컨셉 | 트리거되는 축 |
|---|---|---|
| user_a | 잔고 20만원, 순현금흐름 -50만 | 중도해지/긴급출금 |
| user_b | KB국민은행에 1.3억 집중 | 예금자보호 |
| user_c | 20일 뒤 만기 상품 보유 | 만기/자동재예치 |
| user_d | 카드 승인내역 택시 위주, 대중교통 0건 | 실적조건/우대금리 |

## 3. 백엔드 & AI 아키텍처 (Technical Architecture)

```
[프론트: 상품 ID + 유저 ID]
                      │
                      ▼
[FastAPI 파이프라인 — app/services/rag/pipeline.py]
  ├── 1. 마이데이터 신호 집계 (services/mydata/)
  │      유동성 · 자산분포/만기 · 카드 카테고리 실적
  │
  ├── 2. 검색 쿼리 3계층 생성 (query_builder.py) — LLM 호출 없이 결정론적
  │      ├── 일반 축: 상품 카테고리별 위험 축 (특약/기본약관/약정서에서 검색)
  │      ├── 상품설명서 축: 금리·가입조건 등 사실 정보 (상품설명서에서만)
  │      └── 유저특화 축: 신호가 임계값을 넘을 때만 추가 + "검색된 이유"를 라벨로 전달
  │
  ├── 3. Qdrant 하이브리드 검색 (retrieval.py)
  │      dense(의미) + sparse(BM25, 한국어 형태소) 각각 검색 후 RRF 융합
  │      상품 전용 문서 + 회사 공통 약관(기본약관 등)을 함께 조회
  │
  ├── 4. LLM 추론 (llm_inference.py, gpt-4o-mini, strict JSON Schema)
  │      포인트마다 type(description|risk) · text · detail · clause_index · evidence_quote
  │
  └── 5. 근거 조립 & 검증 (pipeline.py)
         ├── clause_index(번호)로 백엔드가 출처/페이지/PDF키를 결정론적으로 복원
         └── evidence_quote가 조항 원문에 실제로 있는지 대조 — 실패 시 폐기
                      │
                      ▼
[프론트: 상품 정보 / 위험 체크 리포트 + 원문 PDF 하이라이팅]
```

## 4. 기술 스택 & 데이터 (Tech Stack)

### 🛠️ Tech Stack

- **Frontend**: Next.js 16 (App Router), React 19, Tailwind CSS, react-pdf(pdf.js)
- **Backend**: Python FastAPI, OpenAI API (추론 `gpt-4o-mini` / 임베딩 `text-embedding-3-large`)
  - LangChain/LlamaIndex 등 오케스트레이션 프레임워크는 **쓰지 않음** — 근거 검증·쿼리 구성이 전부 커스텀 로직이라 직접 구현이 더 단순함
- **RAG**: Qdrant (dense + sparse 하이브리드, RRF 융합), 한국어 형태소 분석은 kiwipiepy
- **PDF 파싱**: Docling (레이아웃·표 구조 인식) — 색인 시 로컬에서만 실행
- **Object Storage**: MinIO(로컬) / AWS S3(배포) — `STORAGE_BACKEND` env로 전환
- **배포**: 단일 EC2 + Caddy 리버스 프록시(443만 개방, 자동 HTTPS), `deploy/` 하위 별도 compose

### 📊 Data

- **유저**: `app/data/mydata_mock.json` — 마이데이터 표준 API 응답 형식 그대로 (user_a ~ user_d)
- **상품**: 3개 은행(KB국민·신한·하나) 275개 상품 / 색인 청크 약 4,800개
- **문서 종류**: 상품설명서 · 특약 · 기본약관 (회사 공통 약관은 `product_id=None`으로 저장해 공유)

### 현재 미구현 (문서에 넣지 말 것)

- 금감원 오픈 API: 클라이언트/스키마는 있으나 **라우터가 미등록이고 파이프라인에서 안 씀** (`app/api/fss.py`)
- Tavily 웹 검색 Fallback: **구현 없음**

## 5. 기술적 메리트 (Technical Highlights)

**근거 검증으로 환각을 구조적으로 차단**

- 출처·페이지·조항 제목을 LLM이 쓰지 않음. LLM은 검색 결과의 **번호만** 고르고 백엔드가 실제 값을 채움 → 출처 날조가 원천 불가.
- LLM이 낸 인용문이 조항 원문에 실제로 존재하는지 대조하고, 실패하면 버림.
- 프론트가 그 인용문을 **실제 PDF 텍스트 레이어에서 다시 찾아** 하이라이트 → 사용자가 원문으로 직접 대조 가능.

**유저 데이터가 검색 단계부터 반영되는 3계층 쿼리**

- 위험 축을 LLM에 맡기지 않고 신호 임계값 → 축 매핑으로 결정론화. 지연시간이 줄고 동작이 재현 가능해짐.
- 어떤 축이 왜 선택됐는지(유저의 실제 수치)를 LLM 프롬프트에 라벨로 전달 → 리포트 문장에 그 수치가 그대로 반영됨.

**비정형 약관 + 정형 유저 데이터의 결합**

- 단순 If-Else로는 불가능한 약관 문맥을 해석하고, 유저의 실제 소비/자산 수치와 결합해 해당 여부를 판정.

## 6. 심사위원 Q&A 예상 방어 (Winning Points)

**Q. 실제 유저 데이터는 어디서 가져오나요?**
A. 마이데이터 표준 API 규격(계좌 목록/기본·추가정보/거래내역, 카드 승인내역)을 그대로 미러링했고, 인가 제약상 데모에서는 동일 규격의 Mock JSON으로 대체했습니다. 클라이언트/서비스 레이어가 분리돼 있어 실제 API로 교체 시 내부 구현만 바뀝니다.

**Q. LLM이 틀린 근거를 대면 어떻게 하나요?**
A. 세 겹으로 막습니다. ① 출처·페이지는 LLM이 아니라 백엔드가 검색 결과에서 채웁니다. ② LLM이 인용한 문장이 실제 조항 원문에 있는지 문자열로 대조해 실패하면 버립니다. ③ 프론트가 그 문장을 원본 PDF에서 다시 찾아 하이라이트하므로 사용자가 눈으로 검증할 수 있습니다.

**Q. ChatGPT에 약관 PDF를 올리는 것과 뭐가 다른가요?**
A. 두 가지입니다. ① 사용자가 무엇을 물어야 할지 몰라도 됩니다 — 상품을 고르기만 하면 점검 항목이 자동으로 정해집니다. ② 사용자의 자산·소비 데이터가 자동 결합되어, 같은 상품이라도 사람마다 다른 결과가 나옵니다.

## 작업 시 참고

- 의존성 설치는 사용자가 직접 실행함 — `requirements.txt`만 갱신하고 `pip install` 등은 직접 실행하지 말 것.
- 주석은 2줄을 넘지 않도록 주석 사용을 자제할 것. 꼭 필요한 경우에만 간략하게 주석을 남길 것.
- 런타임 의존성은 `requirements.txt`, 크롤링/색인 도구는 `requirements-tools.txt`로 분리돼 있음.
