# 백엔드 빌드 컨텍스트가 repo 루트라, 여기 없는 건 전부 도커 데몬으로 전송된다.
venv/
.git/
.claude/
__pycache__/
*.pyc

# 크롤링/파싱 캐시 (수십 MB, 런타임에 불필요). manifest.json만 Dockerfile에서 COPY한다.
crawled_data/.minio_cache/
crawled_data/.parse_cache/

# 시크릿은 컨테이너에 파일로 들어가지 않는다 (compose env_file로 주입).
.env
deploy/.env

# 백엔드 이미지에 불필요
frontend/
scripts/
results/
memo.md
